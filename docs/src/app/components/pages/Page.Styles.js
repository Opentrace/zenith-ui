import { spacing } from "zenith-ui/styles";
import { palette } from 'zenith-ui/styles/colors';
console.log(palette);

const Styles = {
  base: {
    width: "80%",
    margin: 'auto'
    // padding: `0 ${ spacing.tiny }px`
  },
  header: {
  },
  content: {
  }
};

export default Styles;